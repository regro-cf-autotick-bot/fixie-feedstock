{% set name = "fixie" %}
{% set version = "0.0.8" %}
{% set sha256 = "173315ac54fbf1b997ae010441186c4d13120d357596fd2afb6f4e71d945c937" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 0
  script: python setup.py install --single-version-externally-managed --record record.txt

requirements:
  host:
    - python
    - setuptools
  run:
    - python
    - xonsh
    - cerberus
    - tornado
    - lazyasd

test:
  imports:
    - fixie
    - fixie.environ

about:
  home: http://github.com/ergs/fixie
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE
  summary: 'Cyclus-as-a-Service'

extra:
  recipe-maintainers:
    - scopatz
